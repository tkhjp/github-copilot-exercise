# tc02_judge — copilot_docx  (judgment)

**Question:** この UI 変更要求について、プロダクトマネージャーの立場で判断してください。
既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。

(1) 4 つの変更要求それぞれの実装工数見積もり (Small / Medium / Large) と理由。
(2) 各変更がバックエンド (API) の変更を必要とするか否か。
(3) 最も技術的リスクが高い変更はどれで、その理由は何か。
(4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。

**Source:** Microsoft Copilot web, YYYY-MM-DD, input=tests/text_vs_image/inputs/02_ui_change.docx

## Output

### UI 変更要求の判断について — 前提確認と次のアクション

**要約（ドキュメントからの抜粋）**  
> 「既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。」 

現在の添付ドキュメントには「評価すべき項目（質問）」が記載されていますが、**個々の「4つの赤い吹き出し（＝各変更要求）」の具体内容が含まれていません**。このままでは（1）〜（4）を正確に評価できません。  

---

### 私が今必要としている情報（最短で評価するために）
以下のいずれかを送ってください。受け取り次第、プロダクトマネージャー視点で具体的に回答します。

- **各吹き出しの文言・スクリーンショット**（変更箇所が分かる既存画面と赤吹き出しの注記）  
- または、各変更要求ごとに短く（1〜2行で）次を記載：  
  1. 変更の目的（例：文言修正、レイアウト追加、入力バリデーション、動的フィルタ追加 等）  
  2. 期待される振る舞い（例：クリックでモーダル表示、即時バリデーション、サーバーへ保存 等）  
  3. 優先度（高/中/低）や想定の影響範囲（画面のみ／複数画面／データ構造変更）

---

### 代替案：情報がすぐ出せない場合の暫定対応
もし今すぐ詳細を用意できない場合は、**私が一般的な前提を置いて暫定見積もりを出す**ことも可能です（例：文言のみ／フロントのみのレイアウト変更／新APIが必要なデータ追加／複雑なUX変更）。その場合は、私が採る前提を明示してから **(1) 工数見積 (Small/Medium/Large) と理由、(2) API要否、(3) 技術リスク、(4) 並列実装可否と依存** を提示します。どちらがよいですか？

---

必要な情報を受け取り次第、**プロダクトマネージャー視点での詳細な判断（日本語）**を速やかに提示します。