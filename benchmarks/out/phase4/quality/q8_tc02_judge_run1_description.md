# tc02_judge — q8  run 1/3  (judgment)

**Question:** この UI 変更要求について、プロダクトマネージャーの立場で判断してください。
既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。

(1) 4 つの変更要求それぞれの実装工数見積もり (Small / Medium / Large) と理由。
(2) 各変更がバックエンド (API) の変更を必要とするか否か。
(3) 最も技術的リスクが高い変更はどれで、その理由は何か。
(4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。


**Describe wall_seconds:** 142.09

## Output

プロダクトマネージャーとして、提示されたUI変更要求について分析し、以下の4点に具体的に回答します。

---

## UI変更要求の評価と判断

### (1) 4 つの変更要求それぞれの実装工数見積もりと理由

| 変更要求 | 工数見積もり | 理由 |
| :--- | :--- | :--- |
| **1. 「Remember last search」チェックボックスの追加** | Small | UI要素（チェックボックス）の追加と、その状態をセッションまたはローカルストレージに保存・読み込みする程度のフロントエンドロジックで完結するため。バックエンドへの永続化が必要な場合でも、シンプルなキーバリュー操作であれば工数は小さい。 |
| **2. 検索ボタンの色を青から緑に変更** | Small | 純粋なCSS/スタイリングの変更であり、ビジネスロジックやデータ処理には一切関与しないため。最も実装が容易。 |
| **3. 結果テーブルの右上隅にCSVエクスポートボタンを追加** | Medium | UI要素（ボタン）の追加に加え、そのボタンクリック時に全検索結果データを取得し、サーバーサイドまたはクライアントサイドでCSV形式に変換してダウンロードさせる処理が必要になるため。データ取得とフォーマット処理が関わる。 |
| **4. ページあたりの表示件数を10から20に変更** | Medium | UIの変更（ドロップダウンや設定値の更新）だけでなく、バックエンドAPIに対して「ページサイズ」を制御するパラメータを渡し、サーバー側でデータ取得ロジックを変更する必要があるため。単なるフロントエンドの表示調整以上の影響がある。 |

### (2) 各変更がバックエンド (API) の変更を必要とするか否か

| 変更要求 | バックエンド (API) 変更の要否 | 詳細な理由 |
| :--- | :--- | :--- |
| **1. 「Remember last search」チェックボックス** | **限定的（推奨）** | 単にブラウザセッションで保持するだけであれば不要だが、ユーザーアカウントに関連付けて永続化したい場合は、検索クエリを保存するためのAPIエンドポイントが必要になる。初期段階ではフロントエンドのみで対応可能。 |
| **2. 検索ボタンの色変更** | **不要** | 純粋な見た目の変更（CSS）であり、データ処理ロジックには影響しないため。 |
| **3. CSVエクスポートボタンの追加** | **必要** | 現在のAPIは通常、JSON形式でページネーションされたデータを返しているはず。CSVとして全件または指定範囲を整形して出力するための専用エンドポイント（例: `/api/products/export?query=...`）が必要になる可能性が高い。 |
| **4. ページあたりの表示件数の変更** | **必要** | 現在の検索APIが固定のレコード数でデータを返している場合、このパラメータを受け取り、データベースクエリ（LIMIT句など）を動的に調整するロジックをサーバーサイドに実装する必要があるため。 |

### (3) 最も技術的リスクが高い変更はどれで、その理由は何か。

**最も技術的リスクが高いのは「4. ページあたりの表示件数を10から20に変更」です。**

**理由:**
この変更は、単なるUIの調整ではなく、**データ取得層（バックエンド）の中核的なロジックに影響を与えるため**です。
1. **既存クエリへの影響:** 現在の検索APIがどのようにページネーションを実装しているか（オフセットベースかカーソルベースかなど）によって、変更の難易度が大きく変わります。誤った実装は、データ欠落や重複表示を引き起こします。
2. **パフォーマンスリスク:** ページサイズを増やすことは、一度のリクエストで取得するデータ量が増えることを意味します。もしデータベースクエリが最適化されていない場合、この変更によってAPIのレスポンスタイムが急激に悪化し、システム全体のパフォーマンスボトルネックになる可能性があります。

---

### (4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。

**結論として、一部は並列可能ですが、完全に独立しているわけではありません。**

#### 並列可能なタスク（初期フェーズ）
* **変更 2 (色変更):** 他の作業と全く競合せず、すぐに着手可能です。
* **変更 1 (Remember Search):** フロントエンドでの実装は他の機能に依存しません。

#### 依存関係があるタスク
* **変更 4 $\rightarrow$ 変更 3:** ページサイズ（件数）を変更するロジック（変更 4）が安定し、APIのデータ取得方法が確定しないと、CSVエクスポート（変更 3）で「全件」を取得するための正確なエンドポイントやパラメータ設計ができません。
* **変更 1 $\leftrightarrow$ バックエンド:** もし永続化を伴う場合、変更 1 はバックエンドの認証・データ保存機能に依存します。

#### 推奨される実装順序（リスク低減と効率化のため）

1. **フェーズ 1 (クイックウィン):**
    * 変更 2 (色変更) $\rightarrow$ 最速でリリース可能。
    * 変更 1 (フロントエンドのみのロジック) $\rightarrow$ リスクが低いので並行して進める。
2. **フェーズ 2 (コア機能改修):**
    * **変更 4 (ページサイズ変更)** $\rightarrow$ 最もリスクが高いため、最初にバックエンドで設計・実装し、テストを徹底する。
3. **フェーズ 3 (付加価値機能):**
    * **変更 3 (CSVエクスポート)** $\rightarrow$ 変更 4 でデータ取得の仕組みが確定した後、そのAPIを利用してCSV生成ロジックを構築する。

この順序を取ることで、最も大きな技術的リスク（変更 4）を早期に特定し、他の機能開発をブロックしないようにすることができます。
