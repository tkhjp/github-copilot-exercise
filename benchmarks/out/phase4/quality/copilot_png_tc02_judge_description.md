# tc02_judge — copilot_png  (judgment)

**Question:** この UI 変更要求について、プロダクトマネージャーの立場で判断してください。
既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。

(1) 4 つの変更要求それぞれの実装工数見積もり (Small / Medium / Large) と理由。
(2) 各変更がバックエンド (API) の変更を必要とするか否か。
(3) 最も技術的リスクが高い変更はどれで、その理由は何か。
(4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。

**Source:** Microsoft Copilot web, YYYY-MM-DD, input=tests/text_vs_image/images/02_ui_change.png

## Output

| **変更** | **実装工数** | **API 変更要否** | **主要理由** | **技術リスク** |
| --- | --- | --- | --- | --- |
| **1. 「前回検索を記憶」チェックボックス追加** | **Small** | **No（基本は不要）** | UI とローカル保存（localStorage / cookie）で完結可能 | 低 |
| **2. 検索ボタン色を青→緑に変更** | **Small** | **No** | 単純なスタイル変更（CSS/テーマ） | 低 |
| **3. 結果テーブルの CSV エクスポートボタン追加** | **Medium** | **Maybe（場合によりYes）** | クライアントでの CSV 生成は容易だが、サーバ側でフィルタ・ページングを反映する API が必要なら変更 | 中 |
| **4. 1ページあたり表示件数を10→20に変更** | **Small〜Medium** | **Maybe（ページング API に依存）** | フロントは設定変更のみだが、API が固定 pageSize を想定していると修正必要 | 中 |