# tc02_judge — q5  run 2/3  (judgment)

**Question:** この UI 変更要求について、プロダクトマネージャーの立場で判断してください。
既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。

(1) 4 つの変更要求それぞれの実装工数見積もり (Small / Medium / Large) と理由。
(2) 各変更がバックエンド (API) の変更を必要とするか否か。
(3) 最も技術的リスクが高い変更はどれで、その理由は何か。
(4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。


**Describe wall_seconds:** 100.86

## Output

プロダクトマネージャーとして、提示されたUI変更要求について分析し、以下の4点に具体的に回答します。

---

## UI変更要求の評価と判断

### (1) 4 つの変更要求それぞれの実装工数見積もりと理由

| 要求番号 | 変更内容 | 工数見積もり | 理由 |
| :---: | :--- | :---: | :--- |
| **1** | 「Remember last search」チェックボックスを検索バーの下に追加する。 | **Small** | UI要素の追加（フロントエンドのみ）。既存の検索ロジックや状態管理に小さな変更を加えるだけで済む可能性が高い。 |
| **2** | 検索ボタンの色を青から緑に変更する。 | **Small** | 純粋なCSS/スタイリングの変更であり、ビジネスロジックやデータ処理には一切関与しないため、最も工数が少ない。 |
| **3** | 結果テーブルの右上隅にCSVエクスポートボタンを追加する。 | **Medium** | UI要素の追加に加え、バックエンドAPIを呼び出し、検索結果データをCSV形式で整形・ダウンロードさせるロジックの実装が必要となるため、データ処理と連携が発生し工数が増える。 |
| **4** | ページネーションを現在の10件から20件に変更する。 | **Medium** | フロントエンドの表示ロジック（ページング制御）の変更に加え、バックエンドAPIが「取得件数」を受け取り、それに合わせてデータを返すように調整が必要になるため、フロントとバックの両方に影響を与える。 |

### (2) 各変更がバックエンド (API) の変更を必要とするか否か

| 要求番号 | バックエンド (API) 変更の要否 | 詳細な理由 |
| :---: | :---: | :--- |
| **1** | **No** | これはクライアントサイド（フロントエンド）の状態管理とUI表示に関する機能であり、サーバー側のデータ取得ロジックを変更する必要はない。 |
| **2** | **No** | スタイリングの変更のみであるため、APIへの影響は一切ない。 |
| **3** | **Yes** | CSV形式でのデータ出力処理（データの整形、ファイル生成、ダウンロードストリームの提供）をサーバー側で実装するか、フロントエンドが全データを取得してからクライアントサイドでCSV化するかの設計が必要となる。後者の場合でも、APIから全てのデータを取得する必要があるため、レスポンス仕様の確認・調整は必要。 |
| **4** | **Yes** | 現在のAPIが「1ページあたりN件」というパラメータを受け取っている場合、その`N`（または`pageSize`）のデフォルト値や許容範囲を20に変更する必要があるため、APIエンドポイントの仕様変更が必要となる可能性が高い。 |

### (3) 最も技術的リスクが高い変更はどれで、その理由は何か。

**最も技術的リスクが高いのは「要求 4: ページネーションを現在の10件から20件に変更する」です。**

**理由:**
この変更は、単なるUIの数値変更ではなく、システムの中核的なデータ取得ロジック（バックエンドとフロントエンドの両方）に影響を与えます。

1. **API設計への依存性:** バックエンドがページングをどのように実装しているか（例：オフセットベースかカーソルベースか）によって、単なるパラメータ値の変更で済むのか、それともデータ取得ロジック全体の見直しが必要になるのかが不透明です。
2. **パフォーマンスリスク:** 10件から20件に増やすことで、APIからのレスポンスサイズが増加します。もし検索結果が非常に多い場合、この増加したペイロード（データ量）がネットワーク帯域やサーバーの処理負荷を予期せぬ形で増大させ、レイテンシ悪化を引き起こすリスクがあります。

---

### (4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。

**結論として、大部分は並列実装が可能ですが、「要求 3」と「要求 4」の間には潜在的な依存関係が存在します。**

#### 並列化の可能性:
* **要求 1 (Remember Search) と 要求 2 (Color Change):** これらは完全に独立しており、同時に着手できます（最も安全に並行作業できるタスク）。
* **要求 3 (CSV Export) と 要求 4 (Page Size):** これらの実装は技術的に別々の機能ですが、データ取得の基盤に関わるため、依存関係を考慮する必要があります。

#### 指摘すべき依存関係:

1. **間接的な依存性（最重要）:**
    * **要求 3 (CSV Export) $\rightarrow$ 要求 4 (Page Size):** CSVエクスポート機能は「検索結果の全データ」を取得して処理することが一般的です。もし、現在のAPIがページネーションを強制している場合、CSVエクスポートを行うためには、**一時的に（または設計上）ページング制限を解除し、全てのデータを取得できるエンドポイントが必要になる可能性が高い**です。要求4でページサイズを変更する作業と並行して、この「全件取得」の要件定義と実装を進める必要があります。

2. **直接的な依存性:**
    * 現時点では、他の変更が完了しないと特定の機能が動かないという直接的な依存関係はありませんが、**要求 4 のバックエンド変更（API仕様変更）が完了する前に、要求 3 が「全件取得」を前提として設計を進めると、後で手戻りが発生します。**

#### PMとしての推奨アクション:
まず、最も独立性の高い **(1) と (2)** を最優先で着手させます。次に、**APIチームと連携し、「要求 4 のページサイズ変更」がバックエンドのデータ取得ロジックにどのような影響を与えるか（特に全件取得が可能か）を早期に確認する**ことが、プロジェクト全体のボトルネック解消につながります。
