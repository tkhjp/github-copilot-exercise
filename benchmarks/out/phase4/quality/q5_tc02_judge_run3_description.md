# tc02_judge — q5  run 3/3  (judgment)

**Question:** この UI 変更要求について、プロダクトマネージャーの立場で判断してください。
既存画面と 4 つの赤い吹き出しの変更要求を読み、以下 4 点に具体的に答えてください。

(1) 4 つの変更要求それぞれの実装工数見積もり (Small / Medium / Large) と理由。
(2) 各変更がバックエンド (API) の変更を必要とするか否か。
(3) 最も技術的リスクが高い変更はどれで、その理由は何か。
(4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。


**Describe wall_seconds:** 95.35

## Output

プロダクトマネージャーとして、提示されたUI変更要求について分析し、以下の4点に具体的に回答します。

---

## UI変更要求の分析と判断

### (1) 4 つの変更要求それぞれの実装工数見積もりと理由

| 要求番号 | 変更内容 | 工数見積もり | 理由 |
| :---: | :--- | :--- | :--- |
| **1** | 「Remember last search」チェックボックスを検索バーの下に追加する。 | **Small** | UIの追加要素（チェックボックスとラベル）の配置変更が主であり、ロジックはフロントエンドでの状態管理に留まる可能性が高い。 |
| **2** | 検索ボタンの色を青から緑に変更する。 | **Small** | 純粋なCSS/スタイリングの変更のみであるため、工数は非常に小さい。 |
| **3** | 結果テーブルの右上隅にCSVエクスポートボタンを追加する。 | **Medium** | UI要素の追加に加え、バックエンドAPIを呼び出し、データを取得・整形し、クライアント側でCSV形式に変換してダウンロードさせる処理が必要になるため。 |
| **4** | ページネーションを現在の10件から20件表示に変更する。 | **Medium** | フロントエンドでの表示ロジックの変更に加え、バックエンドAPIに対して「1ページあたりのアイテム数 (per page)」を指定するパラメータの調整（または新規実装）が必要になるため。 |

### (2) 各変更がバックエンド (API) の変更を必要とするか否か

| 要求番号 | バックエンド (API) 変更の要否 | 詳細な理由 |
| :---: | :--- | :--- |
| **1** | **不要（可能性が高い）** | この機能は、ユーザーのローカルストレージやセッション情報に検索クエリを保存するフロントエンド側のロジックで完結できるため。ただし、永続化が必要な場合は軽微なAPI変更が必要になる場合がある。 |
| **2** | **不要** | スタイリング（CSS）のみの変更であるため、バックエンドへの影響はない。 |
| **3** | **必要** | 検索結果データ全体を取得し、それをCSV形式で提供するための専用のエンドポイント、または既存APIにエクスポート機能を追加するロジックが必要となる。 |
| **4** | **必要** | 現在のAPIが固定件数（例：10件）を返している場合、ページネーションパラメータを受け取り、指定された件数分のデータを返すようにサーバーサイドのデータ取得ロジックを変更する必要がある。 |

### (3) 最も技術的リスクが高い変更はどれで、その理由は何か。

**最も技術的リスクが高い変更は「要求 4: ページネーションを現在の10件から20件表示に変更する」です。**

**理由:**
この変更は単なるUIの数値変更ではなく、データ取得と処理の根幹に関わるためです。
1. **API設計への影響:** バックエンドが「オフセット/リミット (Offset/Limit)」や「ページ番号 (Page Number)」といったパラメータを適切に受け取り、データベースクエリ（例: `LIMIT 20 OFFSET X`）を正しく実行できるかどうかに依存します。
2. **パフォーマンスリスク:** データ件数が増えることで、APIのレスポンスタイムが意図せず悪化する可能性があります。特にデータ量が膨大になる場合、サーバーサイドでのクエリ最適化が必要になり、予期せぬボトルネックが発生するリスクがあります。

要求3（CSVエクスポート）もバックエンド変更を伴いますが、これは「特定の操作の結果」としてデータを取得するのに対し、要求4は「データの基本的な表示単位」を変更するため、システム全体のデータフローに影響を与える点でより根本的なリスクが高いと判断します。

### (4) これら 4 つの変更を並列で実装できますか? 依存関係があれば指摘してください。

**結論として、一部の作業は並行可能ですが、完全に独立しているわけではありません。**

#### 並列化可能なタスク（Independent）
* **要求 1 (Remember Search):** フロントエンドでの状態管理とUI配置のみであれば、他の変更とはほぼ独立して進められます。
* **要求 2 (Button Color):** スタイリングの変更であり、完全に独立しています。

#### 依存関係があるタスク（Dependent）
* **要求 3 (CSV Export) $\leftrightarrow$ 要求 4 (Page Size Change):**
    * **強い依存性:** CSVエクスポート機能（Req 3）が「全件」または「現在のページの内容」を対象とする場合、もしAPIがページネーションパラメータを受け取るように変更される（Req 4の実施）のであれば、CSVエクスポートもその新しいデータ取得ロジックを利用するように調整する必要があります。
    * **推奨:** まずは Req 4 を実装し、APIが「件数指定」に対応した状態にしてから、Req 3 の実装に着手するのが最も安全です。

#### 実装順序の提案（Recommended Flow）
1. **フェーズ 1 (低リスク/独立):** 要求 2 $\rightarrow$ 要求 1 を並行して実施し、UIの軽微な改善を完了させる。
2. **フェーズ 2 (基盤変更):** 要求 4 を実装する（APIのデータ取得ロジックを変更）。
3. **フェーズ 3 (機能追加):** 要求 3 を実装する（Req 4で整備された新しいデータ取得メカニズムを利用してCSV生成機能を構築する）。

この順序であれば、技術的リスクを管理しつつ、効率的に開発を進めることができます。
