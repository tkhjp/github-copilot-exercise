この画像は表（テーブル）です。以下の形式で出力してください。

## 表のメタ情報
- タイトル / キャプション（あれば）:
- 行数:
- 列数:

## Markdown テーブル
画像内の表をそのまま Markdown table に変換してください。
- ヘッダー行を `|---|` で区切る
- 各セルの値を可能な限り正確に転写
- セルが空の場合は空欄のまま
- 数値はカンマ区切りや単位もそのまま保持
- マージされたセルがあれば「(merged)」と付記

## 補足
- 強調（太字、色付き、ハイライト）されているセルがあれば箇条書きで指摘
- 表外の注釈・脚注があれば書き出す
